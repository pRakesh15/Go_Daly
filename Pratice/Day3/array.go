package main

//*NOTE- we are not going to use a lot of array instate of array we are with Slice because
// Slice has dynamic length option...

import "fmt"

func main() {
	// fmt.Print("this is a array practice")

	var length int
	fmt.Println("enter the length of the array")
	fmt.Scan(&length)
	var arr  []int

	//make all the input from user for array..

	fmt.Println(arr)

}
